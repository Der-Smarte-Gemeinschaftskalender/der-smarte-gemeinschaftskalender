# Environment Configuration für DSG Kalender
# Kopiere diese Datei nach .env und passe die Werte an deine Umgebung an

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# Laravel Application
APP_NAME="DSG Kalender"
APP_ENV=production
APP_DEBUG=false
APP_KEY=base64:CHANGE_THIS_TO_RANDOM_32_CHARACTER_STRING
APP_URL=https://kalender.example.com
APP_TIMEZONE=Europe/Berlin

# Domain (used by multiple services)
DOMAIN=kalender.example.com

# Admin User Configuration (für automatische User-Erstellung)
ADMIN_EMAIL=admin@${DOMAIN}

# =============================================================================
# FRONTEND (Vue.js) SETTINGS
# =============================================================================

# Vite Frontend URLs
VITE_DSG_API_URL=${APP_URL}/dsgapi
VITE_APP_URL=${APP_URL}
VITE_MOBILIZON_API_URL=${APP_URL}/api

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# PostgreSQL Database (Primary)
DB_CONNECTION=pgsql
DB_HOST=postgres
DB_PORT=5432
DB_DATABASE=laravel
DB_USERNAME=laravel
DB_PASSWORD=secure_random_password_here

# =============================================================================
# CACHE & SESSION CONFIGURATION
# =============================================================================

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=null

# Cache & Sessions (Redis recommended for production)
CACHE_DRIVER=redis
SESSION_DRIVER=redis
QUEUE_CONNECTION=redis

# =============================================================================
# MAIL CONFIGURATION
# =============================================================================

# SMTP Mail Settings
MAIL_MAILER=smtp
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USERNAME=your-email@example.com
MAIL_PASSWORD=your-smtp-password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@${DOMAIN}
MAIL_FROM_NAME=${APP_NAME}

# =============================================================================
# JWT AUTHENTICATION
# =============================================================================

# JWT Secret (für API Authentication)
JWT_SECRET=CHANGE_THIS_TO_SECURE_JWT_SECRET_STRING

# =============================================================================
# SSL/DOMAIN CONFIGURATION
# =============================================================================

# ACME/Certbot Email (uses ADMIN_EMAIL as default)
ACME_EMAIL=${ADMIN_EMAIL}

# =============================================================================
# MOBILIZON CONFIGURATION
# =============================================================================

# Mobilizon Instance Settings
MOBILIZON_INSTANCE_NAME="DSG Mobilizon"
MOBILIZON_HOST=${DOMAIN}
MOBILIZON_EMAIL=${ADMIN_EMAIL}
MOBILIZON_REPLY_EMAIL=noreply@${DOMAIN}

# Mobilizon uses the same MAIL_* settings for SMTP

# =============================================================================
# LOGGING & MONITORING
# =============================================================================

# Logging Configuration
LOG_CHANNEL=daily
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=info

# Queue Settings
QUEUE_FAILED_DRIVER=database

# =============================================================================
# DEVELOPMENT/STAGING EXAMPLE VALUES
# =============================================================================

# Für lokale Entwicklung, ändere diese Werte:
# APP_ENV=local
# APP_DEBUG=true
# APP_URL=http://localhost
# DOMAIN=localhost
# VITE_DSG_API_URL=${APP_URL}:8000/dsgapi
# VITE_APP_URL=${APP_URL}:3000
# VITE_MOBILIZON_API_URL=${APP_URL}:4000/api
# SESSION_SECURE_COOKIE=false
# LOG_LEVEL=debug

# =============================================================================
# WICHTIGE HINWEISE
# =============================================================================

# 1. APP_KEY generieren: docker compose exec backend php artisan key:generate
# 2. JWT_SECRET generieren: docker compose exec backend php artisan jwt:secret
# 3. Sichere Passwörter für DB_PASSWORD verwenden
# 4. DOMAIN und alle URLs auf deine Domain anpassen
# 5. MAIL_* Einstellungen für deinen SMTP-Provider konfigurieren
# 6. ADMIN_EMAIL für automatische Admin-User Erstellung setzen